# Server Configuration
APP_NAME="AI Interview Server"
DEBUG=True
API_V1_STR="/v1"

# Supabase Configuration
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""

# JWT Authentication
# Legacy HS256 secret (fallback — keep during migration)
SUPABASE_JWT_SECRET=""
# JWKS cache TTL in seconds (default: 86400 = 24h)
# JWKS_CACHE_TTL=86400

# Security & CORS
CORS_ORIGINS=["http://localhost:3000"]

# Logging
LOG_LEVEL="info"
